{
  "name": "clasp-template",
  "version": "1.0.0",
  "description": "clasp template",
  "scripts": {
    "setup": "npm run login && npm run create",
    "login": "clasp login",
    "create": "clasp create clasp-template",
    "build": "tsc",
    "push": "npm run build && clasp push",
    "run": "clasp run setScheduling",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sibukixxx",
  "license": "ISC",
  "dependencies": {
    "@google/clasp": "^2.3.0"
  },
  "devDependencies": {
    "@types/google-apps-script": "1.0.10",
    "typescript": "^3.8.2"
  }
}
